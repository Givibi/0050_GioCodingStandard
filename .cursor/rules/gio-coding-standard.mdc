---
description: GioCodingStandard – Giovannis persönliche Erweiterung des LEXI Coding Standards
alwaysApply: true
---

# GioCodingStandard

Dieser Standard **erweitert** den LEXI Coding Standard (`0005_LEXICodingStandard`).
LEXI-Regeln gelten weiterhin vollständig – hier stehen nur Giovannis persönliche Ergänzungen.

## Promotion-Flow

Neue Muster entstehen hier. Wenn sie sich in mindestens einem Projekt bewährt haben,
werden sie in `0005_LEXICodingStandard` übernommen (Promotion).
Das CHANGELOG.md dokumentiert alle Promotions.

---

## Cursor Workspace-Setup

### Grundregel: Eine .code-workspace-Datei pro Projekt

Jedes Projekt bekommt eine `.code-workspace`-Datei im Repo-Root.
Sie ist der **stabile Anker** für die Chat-Geschichte des Projekts.

```json
{
    "folders": [
        { "path": "." },
        { "path": "../GioCodingStandard" }
    ],
    "settings": {}
}
```

> Das Einbinden von `GioCodingStandard` als Workspace-Folder aktiviert diese Regel automatisch.

### Warum das wichtig ist

Cursor speichert Chat-Sessions anhand der Workspace-ID (Hash des Workspace-Pfads).
- Öffnen via **Dock-Icon** → temporäre ID → leere Chat-Liste
- Öffnen via **.code-workspace-Datei** → immer dieselbe ID → alle Chats sichtbar

### Immer so öffnen

```
Finder → Projektordner → Doppelklick auf PROJEKTNAME.code-workspace
```

### Recovery: FIX_CHATS.command

Wenn Chats verschwunden sind:
1. `FIX_CHATS.command` im Repo-Root doppelklicken
2. Cursor schließen wenn aufgefordert (Cmd+Q)
3. Script migriert Chats und öffnet den richtigen Workspace

Template: `GioCodingStandard/templates/new-project/FIX_CHATS.command`

---

## Git Branching & Modul-Abschluss

### Grundregel: Ein Feature-Branch pro Modul

```
main                    ← nur vollständig abgeschlossene, abgenommene Module
Feature_<ModulName>     ← ein Branch pro Modul, gesamte Entwicklung hier
```

Beispiele: `Feature_Quality-Check`, `Feature_KI-Chat`, `Feature_Wissensmanagement`

### Workflow pro Modul

```
1. Branch anlegen:   git checkout -b Feature_<ModulName>
2. Entwickeln:       alle Commits auf dem Feature-Branch
3. Pushen:           git push origin Feature_<ModulName>
4. Modul abgenommen: PR erstellen (siehe unten)
5. Merge → main:     über GitHub.com Pull Request
6. Aufräumen:        Branch Remote + lokal löschen
```

### Pull Request erstellen (GitHub.com empfohlen)

GitHub.com ist besser als GitHub Desktop für PRs – saubere Dokumentation,
alle Commits und Dateien auf einen Blick, Branch-Löschung mit einem Klick.

**Schritte:**
1. github.com/<org>/<repo> öffnen
2. Banner „Compare & pull request" klicken
3. Titel: Conventional Commits Format → `feat: <modul>-Modul vollständig implementiert (Phase 0-6)`
4. Beschreibung: Zusammenfassung (Spec-Phasen, Implementierung, Testergebnis, nächster Schritt)
5. „Create pull request" → „Merge pull request" → „Confirm merge"
6. „Delete branch" klicken

### Nach dem Merge: lokal aufräumen

```bash
git checkout main
git pull origin main
git branch -d Feature_<ModulName>
```

Danach in **GitHub Desktop**: „Fetch origin" klicken.  
In **Cursor**: unten links zeigt `main` – falls nicht, Cursor neu starten.

### PR-Beschreibung Vorlage

```markdown
## Zusammenfassung
Kurze Beschreibung was das Modul macht.

## Spec (Phase 0–5) – alle Phasen abgenommen
- Phase 0: INDEX.md – ...
- Phase 1: ANFORDERUNGEN.md – X User Stories, Y Akzeptanzkriterien
- ...

## Implementierung (Phase 6)
- Modelle, Migration, Services, Routen, Forms, Templates, Tests

## Testergebnis
- pytest: X/X PASSED | ruff: 0 | mypy: 0

## Nächster Schritt
<nächstes Modul> → neuer Branch Feature_<NächstesModul>
```

---

## Neuer Chat pro Modul

### Grundregel

Jedes neue Modul bekommt einen **eigenen Chat** in Cursor.  
Nicht weil der alte Chat verloren geht – sondern weil ein fokussierter Chat besser arbeitet.

**Warum:**
- Kein Kontext-Rauschen aus abgeschlossenen Modulen
- Weniger Token-Verbrauch pro Anfrage
- Saubere Modul-Geschichte (ein Chat = ein Modul)

### Ablauf

```
1. Altes Modul abgeschlossen + gemergt + Branch gelöscht
2. Neuer Branch für nächstes Modul angelegt (git checkout -b Feature_<Modul>)
3. Neuen Chat öffnen: Cursor → Chat-Panel → + Icon
4. NICHT Cursor neu starten – einfach neuen Chat im selben Workspace
5. Rules laden automatisch (alwaysApply: true) – kein manuelles Laden nötig
```

### Voraussetzung: Workspace muss korrekt geöffnet sein

Rules laden nur wenn Cursor über die `.code-workspace`-Datei geöffnet wurde:
```
Finder → Projektordner → Doppelklick auf PROJEKTNAME.code-workspace
```
Wenn Cursor bereits offen ist und der richtige Workspace geladen ist → einfach neuen Chat öffnen, fertig.

### Kein Summary-Chat nötig

Der neue Chat braucht keine Zusammenfassung aus dem alten Chat – weil alles Relevante in den Rules steht:
- `cv-ki-plattform-context.mdc` → Projektstand, alle Entscheidungen, nächster Schritt
- `gio-coding-standard.mdc` → Prozesse, Workflows, Konventionen

**Schnelltest im neuen Chat:** „Was ist der aktuelle Projektstand?" → nennt er die richtigen Module und den nächsten Schritt, sind die Rules korrekt geladen.

---

## Chat ist Protokoll – Rules sind Wissen

Chat-Inhalte sind vergänglich. Wissen gehört in:
- `.cursor/rules/PROJEKTNAME-context.mdc` – Entscheidungen, Stand, offene Punkte
- `Docs/current/` – Specs nach LEXI Spec-First
- `GioCodingStandard/templates/` – wiederverwendbare Muster für neue Projekte

Neues Projekt starten = Rules aus altem Projekt referenzieren, nicht Chat-Geschichte lesen.
